"use client";

// Scheme Management (Pricing Phase 4). Dedicated module page under the order /
// catalogue area. The "Schemes" switch in Catalogue Settings links here.
import { SchemeManager } from "@/components/schemes/scheme-manager";

export default function SchemesPage() {
  return <SchemeManager />;
}
